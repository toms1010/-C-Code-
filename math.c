#include <stdio.h>
#include <math.h>
//#include <stdlib.h>

int main ()
{
    //int x = -1;
    //x=  sqrt(x);

    //x = pow (x , 2);

    float x = 3;
    //x = round(x);
    //x = ceil (x); // for round up
   // x = floor (x); // for round down
   //x = abs(x); 
    x = log(x);

    printf("%f\n", x);



    return 0;

}
