#include <stdio.h>

int main ()
{
    int temp = -5;

    if (temp > 0 || temp < 30)
    {
        printf ("The temperature is BAD ");
    }
    
    else 
    {
        printf ("Them temperature is GOOD");
    }
    return 0;
}